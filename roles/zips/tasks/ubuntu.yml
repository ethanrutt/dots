- name: "Zips | Install zip on Ubuntu"
  become_user: root
  become: true
  ansible.builtin.apt:
    name: zip
    state: present

- name: "Zips | Install unzip on Ubuntu"
  become_user: root
  become: true
  ansible.builtin.apt:
    name: unzip
    state: present
