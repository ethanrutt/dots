- name: "Neovim | Install dependencies on Arch"
  become_user: root
  become: true
  community.general.pacman:
    name:
      - cmake
      - curl
      - unzip
      - ripgrep
      - gcc
    state: present

- name: "Neovim | Install on Arch"
  become_user: root
  become: true
  community.general.pacman:
    name: neovim
    state: present
